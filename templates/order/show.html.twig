{% extends 'base.html.twig' %}

{% block title %}Order{% endblock %}

{% block body %}
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>IdClient</th>
                <td>{{ order.idCommande }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ order.status }}</td>
            </tr>
            <tr>
                <th>Date</th>
                {#<td>{{ order.date ? order.date|date('Y-m-d') : '' }}</td>#}
                <td>{{ order.date }}</td>
            </tr>
            <tr>
                <th>ListeProduit</th>
                {#<td>{{ order.listeProduit ? order.listeProduit|json_encode : '' }}</td>#}
                <td>{{ order.listeProduit }}</td>
            </tr>
            <tr>
                <th>ClientName</th>
                <td>{{ order.clientName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_index') }}">back to list</a>

    {#<a href="{{ path('order_edit', {'id': order.id}) }}">edit</a>#}
    <a href="{{ path('order_product', {'id': order.id}) }}">Mettre à jour les produits</a>

    {{ include('order/_delete_form.html.twig') }}
{% endblock %}
